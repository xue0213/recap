# Config Index

The frozen experiment configuration is declarative in
`rebuttal/baselines/baseline_protocol.py` and
`rebuttal/BASELINE_OFA_REPROTOCOL.md`.

The 12-dataset OFO baseline configuration is frozen in
`rebuttal/ofo_baselines/protocol.py` and
`rebuttal/OFO_12_BASELINE_PROTOCOL.md`.
